/* define a recursive function */
let
  /* calculate n! */
  function fact (n : int) : int =
    if  n = 0
      then 1
      else n * fact (n - 1)
  var t: int := 0
in
  ( t := geti(); printi(fact(t)))
end
